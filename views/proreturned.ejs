<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
    <link href="style.css" type="text/css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <title>Returned items</title>
  </head>
    <body class="main-contents">
      <%- include('partials/nav');%>
      <main class="main-content">
        <h1 style="margin-top: 10px;">Returned Items</h1><br>
        <table class="table table-striped" style="width: 90%;">
            <thead>
                <tr>
                    <th>Invoice</th>
                    <th>SKU</th>
                    <th>Product Name</th>
                    <th>Size US</th>
                    <th>Quantity</th>
                    <th>Unit Price</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <% let totalAmounts = 0 %>
                <% let totalAmount = 0 %>
                <% data.forEach(row => { %>
                <tr>
                    <td><%= row.buyinvoice %></td>
                    <td><%= row.sku %></td>
                    <td><%= row.productname %></td>
                    <td><%= row.size %></td>
                    <td><%= row.totalQuantity %></td>
                    <td><%= row.costprice %></td>
                    <% totalAmount = row.totalQuantity * row.costprice %>
                    <td><%= totalAmount.toFixed(2) %></td>
                    <% totalAmounts += totalAmount %>
                </tr>
                <% }); %>
            </tbody>
            <tfoot>
                <tr>
                    <th colspan="6" style="text-align: right;"><strong>Total:</strong></th>
                    <th>RM<%= totalAmounts.toFixed(2) %></th>
                </tr>
            </tfoot>
        </table>
      </main>
    </body>  
</html>
