<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
        <link href="style.css" type="text/css" rel="stylesheet">
        <title>Bankstatement-Other Debtor Paymentbreakdown</title>
        <script>
            if ( window.history.replaceState ) {
              window.history.replaceState( null, null, window.location.href );
            }
        </script>
    </head>
    <body class="main-contents">
        <%- include('partials/nav'); %>
        <main class="main-content">
            <% if (data.length > 0) { %>
                <h1>Balance Check - Not Settle Yet</h1>
                <table>
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>Date</th>
                      <th>Invoice No</th>
                      <th>Bank</th>
                      <th>Name</th>
                      <th>Amount</th>
                      <th>Detail</th>
                      <th>Settle</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% data.forEach(function(row) { %>
                      <tr>
                        <td><%= row.ID %></td>
                        <td><%= row.Date.toLocaleDateString('en-GB') %></td>
                        <td><%= row.Invoice_No %></td>
                        <td><%= row.Bank %></td>
                        <td><%= row.Name %></td>
                        <td><%= row.Amount %></td>
                        <td><%= row.Detail %></td>
                        <td><%= row.settle %></td>
                      </tr>
                    <% }); %>
                  </tbody>
                </table>
              <% } %>
            <h1>Yong & Yi Other Debtor - Paymentbreakdown</h1>
            <form id="other-form" method="POST" action="/yydebtor" enctype="multipart/form-data">
                <label class="form_name" for="id">ID</label>
                <input class="form_id" type="number" name="id" required><br>
                <label class="form_name" for="date">Date</label>
                <input class="form_id" type="date" name="date" required><br>
                <label class="form_name" for="invoice_no">Invoice No.</label>
                <input class="form_id" type="text" name="invoice_no" id="invoice_no" placeholder="optional"><br>
                <label class="form_name" for="name">Name</label>
                <input class="form_id" type="text" name="name" required><br>
                <label class="form_name" for="amount">Amount</label>
                <input class="form_id" type="number" step="0.01" name="amount" required><br>
                <label class="form_name" for="detail">Details</label><br>
                <textarea class="form_detail" type="text" name="detail"></textarea><br>
                <label class="formfile_name" for="file">File</label>
                <input class="form_file" type="file" name="file" id="file-input"><br><br>
                <div class="button">
                    <button class="form_button" type="submit">Submit</button>
                    <button class="form_button" type="reset">Clear</button>
                </div>
            </form><br>
            <h1>Paymentbreakdown - Record</h1>
            <table class="table table-striped" style="width: 90%;">
                <thead>
                  <tr>
                    <th>ID</th>
                    <th>Date</th>
                    <th>Invoice No</th>
                    <th>Bank</th>
                    <th>Name</th>
                    <th>Amount</th>
                    <th>Detail</th>
                    <th>Settle</th>
                  </tr>
                </thead>
                <tbody>
                  <% paymentData.forEach(function(row) { %>
                    <tr>
                      <td><%= row.ID %></td>
                      <td><%= row.Date.toLocaleDateString('en-GB') %></td>
                      <td><%= row.Invoice_No %></td>
                      <td><%= row.Bank %></td>
                      <td><%= row.Name %></td>
                      <td><%= row.Amount %></td>
                      <td><%= row.Detail %></td>
                      <td><%= row.settle %></td>
                    </tr>
                  <% }); %>
                </tbody>
            </table>
        </main>
        <script>
            document.getElementById('other-form').addEventListener('submit', async (event) => {
          event.preventDefault();
        
          const form = document.getElementById('other-form');
          const formData = new FormData(form);
        
          const fileInput = document.getElementById('file-input');
          const file = fileInput.files[0];
        
          if (!file) {
            alert('Please select a file to upload');
            return;
          }
        
          try {
            // Send form data to your server-side code
            await fetch('/yydebtor', {
              method: 'POST',
              body: formData,
            });
        
            // Get the invoice number from the form
            const invoiceNumber = document.getElementById('invoice_no').value;
        
            // Send the image file to Discord
            const discordFormData = new FormData();
            discordFormData.append('file', file, file.name);
            discordFormData.append('payload_json', JSON.stringify({
              content: `Invoice Number: ${invoiceNumber}`,
            }));
        
            await fetch('https://discord.com/api/webhooks/1111598529685766184/kuAS0RNLZ50M5JfBrqBPffEHALSYtTOKZOhR92F2uJ6D3mBtKsGHRTOege-JexBQxhLY', {
              method: 'POST',
              body: discordFormData,
            });
        
            alert('Data saved and file sent to Discord');
            location.reload();
          } catch (error) {
            console.error(error);
            alert('An error occurred');
          }
        });
        </script>
    </body>
</html>