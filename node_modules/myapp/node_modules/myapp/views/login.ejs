<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Login Page</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <style>
      body {
        background-color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        user-select: none;
      }
      .container {
        text-align: center;
        position: relative;
        max-width: 400px;
        width: 100%;
      }
      h2 {
        font-size: 24px;
        font-family: Arial, Helvetica, sans-serif;
        font-weight: bold;
        margin-bottom: 20px;
        color: #333;
      }
      form {
        margin-top: 20px;
        max-width: 300px;
        margin: auto;
      }
      .labelss {
        font-family: Arial, Helvetica, sans-serif;
        font-weight: 600;
        display: block;
        text-align: left;
        margin-bottom: 5px;
      }
      .form-control {
        margin-bottom: 15px;
        box-shadow: 0 4px 8px;
      }
      .error-message {
        color: red;
        font-size: 14px;
        margin-bottom: 10px;
      }
      .btn-container {
        display: flex;
        justify-content: space-between; /* Align buttons side by side with space between */
        width: 100%; /* Make sure the container takes full width */
        max-width: 300px; /* Limit the width of the buttons container */
        margin: 0 auto; /* Center the buttons container */
      }

      .overlay-image {
        width: 120px; /* Adjust the size of the logo */
        height: auto;
        margin: 0 auto; /* Center the image */
        display: block;
      }
      .btn {
        font-size: 18px;
        border-radius: 6px;
        margin-top: 10px;
        width: 100%; /* Make buttons take full width */
      }
      .signup-link {
        margin-top: 10px;
        font-size: 14px;
        color: #555;
      }
    </style>
    <script>
      if (window.history.replaceState) {
          // Prevents the form from being resubmitted on page refresh
          window.history.replaceState(null, null, window.location.href);
      }
    </script>
  </head>
  <body>
    <div class="container">
      <img src="logo.png" alt="Overlay Image" class="overlay-image">
      <h2>Welcome Back to Inventory System v1.0</h2>
      <form method="POST" action="/login">
        <div>
          <label class="labelss" for="username">Username:</label>
          <input class="form-control" type="text" id="username" name="username" required>
        </div>
        <div>
          <label class="labelss" for="password">Password:</label>
          <input class="form-control" type="password" id="password" name="password" required>
        </div>
        <% if (locals.error) { %>
          <p class="error-message"><%= locals.error %></p>
        <% } %>
        <div class="btn-container">
          <!-- Login Button -->
          <button type="submit" class="btn btn-outline-dark">Login</button>
          <!-- Sign Up Button -->
          <a href="/register" class="btn btn-outline-secondary">Sign Up</a>
        </div>
      </form>
    </div>
  </body>
</html>
